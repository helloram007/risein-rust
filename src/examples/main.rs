mod examples; // Declare the `examples` module

fn main() {
    // Call teh function or code inside `examples.rs` (adjust this based on what's in your examples.rs)
    examples::example::my_abc_fn();
}